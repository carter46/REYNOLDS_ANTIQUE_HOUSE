# Risks and blockers

1. **Missing archive banners** (`img/banner/**`, sell-consign JPGs, auction GIF) — cannot pixel-match commented flexslider; use active `.newel-hero` + CSS estate band + local product imagery.
2. **Historical copy** — Jackie Kennedy / “Where you've seen Newel” / press names need management decision; must not delete sections.
3. **Fonts** — Google Fonts not mirrored; Typekit Astoria is. Self-host or document fallback gaps.
4. **Mega IA depth** — Archive has hundreds of category HTML pages; working catalog is flat JSON categories — map mega links to filters, do not rebuild category HTML forest.
5. **Estate Wix** — Offline Thunderbolt limits remain; restore chrome around existing body.
6. **Shared PDP** — Must not be abandoned for per-product HTML.
7. **Commerce removal** — Account/Saved/Cart must stay out functionally but **spacing** should remain so logo stays centered.
8. **Deployed site** — Local restore does not fix https://reynoldsantiquehouse.com/ until redeploy (Phase E checks both).
9. **Searchspring** — Never reconnect; search UI must still appear.
10. **Legal/brand** — Do not ship Newel wordmark; use `reynolds_logoe.png`.
