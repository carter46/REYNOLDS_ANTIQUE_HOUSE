# Local / secrets — real SMTP credentials (upload manually to server)
includes/mail-config.php
.env
.env.*
!.env.example

# Runtime inquiry rate-limit / logs (keep .htaccess + .gitkeep)
includes/storage/*.json
includes/storage/*.log

# Local backups (not for version control)
backup/
backup/**

# OS / editor
.DS_Store
Thumbs.db
*.log
